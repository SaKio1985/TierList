{
  "name": "tierlist0",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "cross-env NODE_ENV=development rspack serve",
    "build": "cross-env NODE_ENV=production rspack build"
  },
  "devDependencies": {
    "@biomejs/biome": "1.8.3",
    "@rspack/cli": "^1.0.0",
    "@rspack/core": "^1.0.0",
    "cross-env": "^7.0.3"
  },
  "dependencies": {
    "tierlist0": "."
  }
}
